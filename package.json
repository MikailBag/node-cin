{
  "name": "node-cin",
  "version": "1.0.0",
  "description": "sync read from stdin",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "gulp build",
    "postinstall": "npm run build"
  },
  "keywords": [
    "sync",
    "io"
  ],
  "author": "MikailBag",
  "license": "MIT",
  "dependencies": {
    "bindings": "1.2.1",
    "gulp": "github:gulpjs/gulp#4.0",
    "node-gyp": "^3.3.1"
  },
  "repository": "https://github.com/MikailBag/node-cin",
  "devDependencies": {
    "del": "^2.2.1"
  }
}
