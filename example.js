"use strict";

const cin = require(".");
let userName,userAge;
console.log("hi! What's your name?");
userName = cin();
console.log(`So, ${userName}, how old are you?`);
userAge = parseInt(cin());
console.log(
`running
db.push(
    {
        "name": ${userName},
        "age": ${userAge}
    }
:))))))));`);